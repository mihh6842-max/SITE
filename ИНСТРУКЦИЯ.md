# 📋 Инструкция по настройке обменника EXMO кодов

## ✅ Что уже готово

### Сайт:
- ✅ Главная страница (index.html)
- ✅ Форма обмена (sell.html)
- ✅ Страница условий обмена (terms.html)
- ✅ Политика конфиденциальности (privacy.html)
- ✅ Только режим продажи кодов (режим покупки удален)
- ✅ Курс обмена 1×1
- ✅ Режим работы 24/7
- ✅ Интеграция с Telegram

### Telegram бот:
- ✅ Прием уведомлений о заявках
- ✅ Сохранение в базу данных (data/orders.json)
- ✅ Команды /start и /orders
- ✅ Готов для хостинга

---

## 🚀 Настройка (5 минут)

### Шаг 1: Получите ваш Telegram ID

1. Откройте Telegram
2. Найдите бота: `@userinfobot`
3. Отправьте команду `/start`
4. Скопируйте ваш ID (например: `123456789`)

### Шаг 2: Настройте бота

Откройте файл `telegram-bot/config.py`:

```python
# Было:
ADMIN_ID = None

# Должно быть:
ADMIN_ID = 123456789  # Ваш ID из userinfobot
```

### Шаг 3: Настройте сайт

Откройте файл `sell.html`, найдите строку 190:

```javascript
// Было:
const CHAT_ID = 'YOUR_TELEGRAM_ID';

// Должно быть:
const CHAT_ID = '123456789';  // Ваш Telegram ID
```

### Шаг 4: Запустите бота

#### Windows:
```bash
cd telegram-bot
pip install requests
python bot.py
```

#### Linux/Mac:
```bash
cd telegram-bot
pip3 install requests
python3 bot.py
```

Вы увидите:
```
Bot started...
Connected as: @ваш_бот
```

### Шаг 5: Тестирование

1. Откройте `index.html` в браузере
2. Нажмите "Продать EXMO код"
3. Заполните форму тестовыми данными:
   - Код: TEST-1234-5678
   - Сумма: 5000
   - ФИО: Тест Тестов
   - Телефон: +7 999 123-45-67
   - Банк: Тинькофф
4. Нажмите "Создать заявку"
5. Проверьте Telegram - должно прийти уведомление

---

## 📱 Работа с ботом

### Команды:
- `/start` - Показать ваш Telegram ID
- `/orders` - Показать последние 5 заявок

### Формат уведомления:
```
📋 Новая заявка на обмен

🔹 ID заявки: 1
🔹 EXMO код: TEST-1234-5678
🔹 Сумма: 5000 руб.
🔹 К получению: 5000 руб.

👤 Данные клиента:
• ФИО: Тест Тестов
• Телефон: +7 999 123-45-67
• Банк: Тинькофф

⏰ Дата: 2026-01-13T12:00:00.000Z
```

---

## 🌐 Хостинг

### Вариант 1: VPS/Сервер (Linux)

```bash
# Загрузите файлы на сервер
scp -r telegram-bot user@server:/home/user/

# Подключитесь к серверу
ssh user@server

# Установите зависимости
cd telegram-bot
pip3 install -r requirements.txt

# Запустите бота в фоне
nohup python3 bot.py > bot.log 2>&1 &

# Проверьте статус
tail -f bot.log
```

### Вариант 2: systemd (рекомендуется)

Создайте файл `/etc/systemd/system/exmo-bot.service`:

```ini
[Unit]
Description=EXMO Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/home/user/telegram-bot
ExecStart=/usr/bin/python3 bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable exmo-bot
sudo systemctl start exmo-bot
sudo systemctl status exmo-bot
```

### Вариант 3: Heroku

1. Установите Heroku CLI
2. Создайте `Procfile` в папке telegram-bot:
```
worker: python bot.py
```

3. Деплой:
```bash
cd telegram-bot
heroku create
git init
git add .
git commit -m "Initial commit"
git push heroku master
heroku ps:scale worker=1
```

### Вариант 4: PythonAnywhere

1. Зарегистрируйтесь на pythonanywhere.com
2. Загрузите папку telegram-bot
3. Откройте консоль Bash
4. Установите зависимости: `pip3 install requests`
5. В разделе "Tasks" создайте Always-on task: `python3 /home/user/telegram-bot/bot.py`

---

## 🛡️ Безопасность

### ⚠️ Важно:

1. **Никогда не публикуйте токен бота**
   - Не загружайте `config.py` в Git
   - Не показывайте токен на скриншотах

2. **Используйте .gitignore**
   ```
   telegram-bot/config.py
   telegram-bot/data/
   ```

3. **На продакшн-сервере используйте переменные окружения**
   ```python
   import os
   BOT_TOKEN = os.getenv('BOT_TOKEN', 'fallback_token')
   ```

---

## 🔧 Устранение проблем

### Бот не запускается
```bash
# Проверьте установку requests
pip install requests

# Проверьте Python версию (нужно 3.7+)
python --version
```

### Нет уведомлений в Telegram
1. Проверьте ADMIN_ID в config.py
2. Проверьте CHAT_ID в sell.html (должен совпадать)
3. Убедитесь, что бот запущен

### Ошибка "codec can't encode"
- Уже исправлено в bot.py
- Если возникает: используйте английские символы в print()

### Бот не отвечает на команды
1. Проверьте токен в config.py
2. Убедитесь, что бот не заблокирован
3. Перезапустите бота

---

## 📊 База данных

Все заявки сохраняются в `telegram-bot/data/orders.json`:

```json
[
  {
    "id": 1,
    "type": "sell",
    "exmoCode": "TEST-1234-5678",
    "giveAmount": 5000,
    "receiveAmount": 5000,
    "fullName": "Тест Тестов",
    "phone": "+7 999 123-45-67",
    "bank": "Тинькофф",
    "status": "pending",
    "createdAt": "2026-01-13T12:00:00.000Z"
  }
]
```

---

## 📞 Контакты

Не забудьте обновить контакты на сайте:

1. **В шапке** (index.html и sell.html):
   ```html
   <span>📱 Поддержка: <a href="https://t.me/yourusername">@yourusername</a></span>
   <span>📧 Email: support@example.com</span>
   ```

2. **В футере** (все страницы):
   ```html
   <p>📱 Telegram: <a href="https://t.me/yourusername">@yourusername</a></p>
   <p>📧 Email: support@example.com</p>
   ```

Замените на реальные контакты.

---

## ✅ Чеклист запуска

- [ ] Получен Telegram ID через @userinfobot
- [ ] Настроен config.py (ADMIN_ID)
- [ ] Настроен sell.html (CHAT_ID)
- [ ] Установлен requests
- [ ] Запущен бот (python bot.py)
- [ ] Протестирована отправка заявки
- [ ] Получено уведомление в Telegram
- [ ] Обновлены контакты на сайте
- [ ] Бот запущен на хостинге
- [ ] Сайт загружен на хостинг

---

## 🎉 Готово!

Обменник настроен и готов к работе!

При вопросах проверьте:
- telegram-bot/README.md - подробная документация
- telegram-bot/SETUP.md - быстрая настройка
- telegram-bot/api.py - примеры интеграции
