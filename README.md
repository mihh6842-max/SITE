# EXMO Маркетплейс - Платформа для торговли EXMO кодами

Полнофункциональный маркетплейс для покупки и продажи EXMO кодов с системой регистрации, корзиной и управлением заказами.

## Структура проекта

### Страницы:
- `register.html` - Регистрация (телефон, email, пароль)
- `login.html` - Вход в систему
- `shop.php` - Главная страница магазина с товарами
- `sell.php` - Форма добавления товара на продажу
- `index.html` - Старая версия (можно удалить)

### PHP обработчики:
- `db.php` - Подключение к БД
- `register_process.php` - Обработка регистрации
- `login_process.php` - Обработка входа
- `sell_process.php` - Добавление товара
- `checkout.php` - Оформление заказа
- `logout.php` - Выход из системы

### База данных:
- `database.sql` - SQL схема БД
- Таблицы: users, products, orders

### Frontend:
- `style.css` - Все стили
- `shop.js` - JavaScript для корзины
- `script.js` - Общие функции
- `19264a464315513c06c2dc242649e340.mp4` - Видео фон

## Установка

### 1. Создать БД:

```sql
# Импортировать database.sql в MySQL
mysql -u root -p < database.sql
```

Или через phpMyAdmin:
1. Создать БД `exmo_marketplace`
2. Импортировать `database.sql`

### 2. Настроить подключение:

Открыть `db.php` и изменить данные подключения:

```php
$host = 'localhost';
$dbname = 'exmo_marketplace';
$username = 'root';      // Ваш MySQL юзер
$password = '';          // Ваш MySQL пароль
```

### 3. Запустить:

**OpenServer/Denwer:**
- Скопировать в `domains/`
- Открыть: `http://localhost/register.html`

**Встроенный PHP сервер:**
```bash
php -S localhost:8000
```
Открыть: `http://localhost:8000/register.html`

## Использование

### Регистрация:
1. Перейти на `register.html`
2. Ввести телефон, email, пароль
3. Подтвердить пароль
4. Зарегистрироваться

### Вход:
1. Перейти на `login.html`
2. Ввести email и пароль
3. Войти → перенаправление в магазин

### Продажа ключа:
1. Войти в систему
2. Нажать "Продать ключ"
3. Заполнить форму:
   - Название товара
   - Описание
   - EXMO код
   - Цена
   - Платежные данные (телефон СБП, банк)
4. Выставить на продажу

### Покупка:
1. Просмотреть товары в магазине
2. Добавить в корзину
3. Открыть корзину (иконка 🛒)
4. Оформить заказ
5. Товар помечается как проданный

## Функции

✅ Регистрация/Вход с проверкой
✅ Видео фон (анимированный градиент)
✅ Магазин с карточками товаров
✅ Корзина (LocalStorage)
✅ Система заказов
✅ Статусы товаров (активный/продан)
✅ Темный дизайн в стиле обменника
✅ Адаптивный дизайн

## Безопасность

- Пароли хэшируются (password_hash)
- Защита от SQL-инъекций (PDO prepared statements)
- Валидация email
- Проверка длины пароля
- Сессии для аутентификации

## База данных

### Таблица `users`:
- id, phone, email, password_hash, created_at

### Таблица `products`:
- id, seller_id, title, description, exmo_code, price
- payment_phone, payment_bank, status, created_at, sold_at

### Таблица `orders`:
- id, buyer_id, product_id, status, created_at, completed_at

## Дизайн

- Шрифт: Rubik
- Цвета: Черный фон, оранжево-желтые градиенты
- Скругленные углы
- Полупрозрачные элементы с backdrop-filter
- Hover эффекты
- Анимации

## Дополнительно

Для расширения функционала можно добавить:
- Email уведомления при заказе
- Telegram бот для уведомлений
- История заказов пользователя
- Рейтинг продавцов
- Поиск и фильтры товаров
- Админ панель
